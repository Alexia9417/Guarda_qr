import 'package:flutter/material.dart';

class AppColors {
  static const azul = Color(0xFF003466);
  static const rojo = Color(0xFFE2042D);

  // escala opcional
  static const fondo = Colors.white;
  static const texto = azul;
}
